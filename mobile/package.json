{
  "name": "fda-forever-doesnt-allow",
  "version": "1.0.0",
  "private": true,
  "description": "FDA: Forever Doesn't Allow — Android app wrapper for the Aileron Games title",
  "type": "module",
  "scripts": {
    "fonts": "node tools/fetch-fonts.mjs",
    "build": "node tools/build-www.mjs",
    "sync": "npm run build && cap sync android",
    "apk:debug": "npm run sync && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run sync && cd android && ./gradlew assembleRelease",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/browser": "^6.0.4",
    "@capacitor/core": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
